# -*- coding: utf-8 -*-
"""Functions to call when running the function.

This module should contain a function called `run_module`, that is executed
when the module is run with `python -m delphi_nowcast`.
"""


def run_module():
    """
    Skeleton to run delphi_nowcast indicator.

    Returns
    -------
    prints the updated signal names
    """
    # params = read_params()
    return
