# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python package

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        packages: [usafacts]
#        packages: [_delphi_utils_python, cdc_covidnet, claims_hosp, combo_cases_and_deaths, jhu, nchs_mortality, quidel, quidel_covidtest, safegraph, safegraph_patterns, usafacts]
    defaults:
      run:
        working-directory: ${{ matrix.packages }}
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install utils
      if: ${{ matrix.packages }} != '_delphi_utils_python'
      run: |
        python -m pip install --upgrade pip
        pip install ../_delphi_utils_python/
    - name: Install package and dependencies
      run: |
        python -m pip install --upgrade pip
        pip install .
        pip install pylint pytest
    - name: Lint with pylint
      run: |
        pylint --version
        pylint *delphi_* –-optimize-ast=y
    - name: Test with pytest
      run: |
        (cd tests && pytest)
